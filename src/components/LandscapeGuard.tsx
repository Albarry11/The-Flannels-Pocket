import React from 'react';

// Mobile views are now responsive across portrait and landscape.
// Non-blocking guard kept as a no-op placeholder.
export const LandscapeGuard: React.FC = () => {
  return null;
};
